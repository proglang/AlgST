fib1 : Int -> Int
fib1 =
  rec fib : Int -> Int = \(n:Int) ->
    if n == 1 then 1 else fib (n - 1) + fib (n - 2)

fib2 : Int -> Int
fib2 =
  let rec fib : Int -> Int = \(n:Int) ->
    if n == 1 then 1 else fib (n - 1) + fib (n - 2)
  in
  fib
